import { defaultRoutes, defaultRoutes2, defaultRoutes3} from "../router/navList";

export const routeType = {
  1: defaultRoutes, // 村落保护
  2: defaultRoutes2, // 未来乡村申报
  3: defaultRoutes3, // 未来乡村小程序
}